import { fillPresets } from "./presets"

const presetListSelector: Element = document.querySelector(".about__preset")

fillPresets(presetListSelector)

console.log(document.querySelector(".reqForm"))
